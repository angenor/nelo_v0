# Membre déclaratif de l'espace de travail : non installé, il déclare ses dépendances internes.
[project]
name = "nelo-socle-personnes"
version = "0.1.0"
description = "Le noyau des personnes : nom, prénoms, langue"
requires-python = "==3.14.*"
dependencies = [
    "nelo-shared",
]

[tool.uv]
package = false

[tool.uv.sources]
nelo-shared = { workspace = true }
