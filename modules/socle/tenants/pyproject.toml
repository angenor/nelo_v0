# Membre déclaratif de l'espace de travail : non installé, il déclare ses dépendances internes.
[project]
name = "nelo-socle-tenants"
version = "0.1.0"
description = "Le module doré — tenants, établissements, catalogue de paramètres"
requires-python = "==3.14.*"
dependencies = [
    "nelo-shared",
    "nelo-domaine",
]

[tool.uv]
package = false

[tool.uv.sources]
nelo-shared = { workspace = true }
nelo-domaine = { workspace = true }
