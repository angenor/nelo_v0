# Membre déclaratif de l'espace de travail : non installé, il déclare ses dépendances internes.
[project]
name = "nelo-metier-protection"
version = "0.1.0"
description = "La protection de l'enfance — CLOISONNÉ"
requires-python = "==3.14.*"
dependencies = [
    "nelo-shared",
    "nelo-domaine",
]

[tool.uv]
package = false

[tool.uv.sources]
nelo-shared = { workspace = true }
nelo-domaine = { workspace = true }
